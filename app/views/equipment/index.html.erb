<div class="container-winter">
  <h1 class="title">Hi there 👋🏼,</h1>
  <h1 class="title">Welcome on your equipment index !!</h1>
  <div class="item">
    <% @equipment.each do |item| %>
      <div class="card-product">
        <%= cl_image_tag item.photo, crop: :fill, height: 100 %>
        <div class="card-product-infos">
          <h2><%= link_to item.name, equipment_path(item) %></h2>
          <p><%= item.description %></p>
        </div>
      </div>
    <% end %>
  </div>
  <%= link_to 'Add an equipment', new_owner_equipment_path, class: "button" %>
</div>
