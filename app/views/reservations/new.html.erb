<div class="container">
  <div class="form-new-reservation">
    <p><%= @equipment.name %></p>
    <p><%= @equipment.description %></p>
    <p id="price-per-day"><%= @equipment.price_per_day %>€</p>
    </ul>
    <%= simple_form_for [@equipment, @reservation] do |f| %>
    <%= f.input :start_date, input_html: { id: 'start-date' } %>
    <%= f.input :end_date, input_html: { id: 'end-date' } %>
    <%= f.submit %>
    <% end %>
  <%= link_to 'Back', equipment_path(@equipment) %>
  </div>
</div>


